{
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "typescript": "~4.7.0",
    "vitest": "^0.27.1"
  },
  "dependencies": {
    "temperaments": "^0.4.4",
    "xen-dev-utils": "^0.1.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "test": "vitest",
    "preprocess": "npm run compile",
    "process": "node build/scripts/process.js"
  }
}
